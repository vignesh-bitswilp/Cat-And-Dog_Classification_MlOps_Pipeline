apiVersion: v1
kind: Service
metadata:
  name: cats-dogs-inference-svc
  labels:
    app: cats-dogs-inference
spec:
  type: ClusterIP
  selector:
    app: cats-dogs-inference
  ports:
    - name: http
      port: 80
      targetPort: 8000
